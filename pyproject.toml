[project]
name = "growl"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "attrs>=26.1.0",
    "docker>=7.2.0",
    "h5py>=3.16.0",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "pandas>=3.0.3",
    "pint>=0.25.3",
    "uncertainties>=3.2.3",
]

[project.scripts]
parse-compas-options = "growl.tools.parse_compas_options:main"
plot-popsynth = "growl.tools.plot_popsynth:main"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pandas-stubs>=3.0.3.260530",
    "pre-commit>=4.6.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff.lint]
select = ["I"]

[tool.pyright]
venv = ".venv"
venvPath = "."
